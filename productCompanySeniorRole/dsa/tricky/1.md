Locked 👍
Starting from **Topic 1** exactly as agreed.

---

## 📄 **File name:** `java-core-dsa.md`

# **DSA Questions – Java Language Core (Primitives & Evaluation Semantics)**

---

### **DSA 1**

```java
int x = Integer.MAX_VALUE + 1;
System.out.println(x);
```

**Answer:**
`-2147483648`

**Explanation:**
`int` overflows silently using 2’s complement.

---

### **DSA 2**

```java
System.out.println(Math.abs(Integer.MIN_VALUE));
```

**Answer:**
`-2147483648`

**Explanation:**
`+2^31` cannot be represented in `int`, so overflow occurs.

---

### **DSA 3**

```java
System.out.println(Math.addExact(Integer.MAX_VALUE, 1));
```

**Answer:**
`ArithmeticException`

**Explanation:**
`addExact` checks overflow explicitly.

---

### **DSA 4**

```java
System.out.println(0.1 + 0.2 == 0.3);
```

**Answer:**
`false`

**Explanation:**
Binary floating-point cannot exactly represent decimal fractions.

---

### **DSA 5**

```java
System.out.println(10 / 0.0);
System.out.println(0.0 / 0.0);
```

**Answer:**

```
Infinity
NaN
```

**Explanation:**
Java follows IEEE-754 for floating-point arithmetic.

---

### **DSA 6**

```java
System.out.println(-7 / 2);
```

**Answer:**
`-3`

**Explanation:**
Integer division truncates toward zero.

---

### **DSA 7**

```java
byte b = 127;
b++;
System.out.println(b);
```

**Answer:**
`-128`

**Explanation:**
`byte` is promoted to `int`; overflow occurs before casting back.

---

### **DSA 8**

```java
byte b = 10;
b = b + 1;
System.out.println(b);
```

**Answer:**
Compile-time error

**Explanation:**
`b + 1` is evaluated as `int`; explicit cast is required.

---

### **DSA 9**

```java
long l = 3_000_000_000L;
System.out.println((int) l);
```

**Answer:**
`-1294967296`

**Explanation:**
Narrowing conversion truncates higher-order bits.

---

### **DSA 10**

```java
char c = 65535;
c++;
System.out.println((int) c);
```

**Answer:**
`0`

**Explanation:**
`char` is an unsigned 16-bit value and wraps around.

---

### **DSA 11**

```java
int x = 5;
System.out.println(x++ + ++x);
```

**Answer:**
`12`

**Explanation:**
Java evaluates left-to-right; post-increment returns old value.

---

### **DSA 12**

```java
int x = 0;
System.out.println(true || (++x > 0));
System.out.println(x);
```

**Answer:**

```
true
0
```

**Explanation:**
`||` short-circuits; right side is not evaluated.

---

When you’re ready, send **`2`** and I’ll generate **Topic 2 – Method Resolution & Overloading** with the same strict format and a simple file name.
