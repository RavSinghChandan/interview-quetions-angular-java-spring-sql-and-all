Locked ✅
Finishing the roadmap with **Topic 15** in the **same colorful `.md`**, **strict DSA**, **screen-share interview–ready** format.

---

## 📄 **File name:** `java-defensive-dsa.md`

# 🧠 **DSA Questions – Coding Style & Defensive Java (Production Readiness)**

*(Senior Backend | Barclays / Enterprise Java)*

> ⚠️ This topic shows whether you write **safe, maintainable, production-grade Java**.

---

## 🎯 DSA 1 — Null-safety first

```java
String s = null;
System.out.println(s.length());
```

**Answer:**

```
NullPointerException
```

**Explanation:**
Accessing methods on `null` crashes immediately.

---

## 🎯 DSA 2 — Defensive null check

```java
String s = null;
System.out.println(s == null ? 0 : s.length());
```

**Answer:**

```
0
```

**Explanation:**
Defensive checks prevent runtime failures.

---

## 🎯 DSA 3 — `Objects.requireNonNull`

```java
String s = null;
Objects.requireNonNull(s);
```

**Answer:**

```
NullPointerException
```

**Explanation:**
Fail-fast with clear intent and stack trace.

---

## 🎯 DSA 4 — Constructor validation

```java
class User {
    User(String name) {
        this.name = name;
    }
}
```

**Answer:**

```
Bug-prone
```

**Explanation:**
Invalid state allowed; constructor should validate inputs.

---

## 🎯 DSA 5 — Defensive constructor

```java
class User {
    User(String name) {
        this.name = Objects.requireNonNull(name);
    }
}
```

**Answer:**

```
Safe construction
```

**Explanation:**
Prevents creation of invalid objects.

---

## 🎯 DSA 6 — Exposing internal state (danger)

```java
class A {
    List<Integer> list = new ArrayList<>();
    List<Integer> get() { return list; }
}
```

**Answer:**

```
Encapsulation broken
```

**Explanation:**
Caller can mutate internal state.

---

## 🎯 DSA 7 — Defensive copy

```java
class A {
    List<Integer> list = new ArrayList<>();
    List<Integer> get() { return new ArrayList<>(list); }
}
```

**Answer:**

```
State protected
```

**Explanation:**
Returns a copy instead of internal reference.

---

## 🎯 DSA 8 — Mutable fields in public API

```java
public int count;
```

**Answer:**

```
Bad design
```

**Explanation:**
Public mutable state breaks invariants.

---

## 🎯 DSA 9 — Immutability pattern

```java
final class User {
    private final int id;
    User(int id) { this.id = id; }
}
```

**Answer:**

```
Immutable & thread-safe
```

**Explanation:**
Immutability simplifies correctness and concurrency.

---

## 🎯 DSA 10 — Returning `null` from API

```java
User find(int id) {
    return null;
}
```

**Answer:**

```
Risky API
```

**Explanation:**
Forces callers to add defensive null checks everywhere.

---

## 🎯 DSA 11 — Better API return

```java
Optional<User> find(int id) {
    return Optional.empty();
}
```

**Answer:**

```
Clear contract
```

**Explanation:**
Explicit absence improves readability and safety.

---

## 🎯 DSA 12 — Magic numbers

```java
if (status == 3) { }
```

**Answer:**

```
Unclear intent
```

**Explanation:**
Magic numbers reduce maintainability.

---

## 🎯 DSA 13 — Named constants

```java
static final int STATUS_ACTIVE = 3;
```

**Answer:**

```
Readable & maintainable
```

**Explanation:**
Self-documenting code reduces bugs.

---

## 🎯 DSA 14 — Silent exception handling

```java
try {
    risky();
} catch (Exception e) {
}
```

**Answer:**

```
Production smell
```

**Explanation:**
Swallowing exceptions hides failures.

---

## 🎯 DSA 15 — Fail-fast error handling

```java
try {
    risky();
} catch (Exception e) {
    throw new RuntimeException(e);
}
```

**Answer:**

```
Failure visible
```

**Explanation:**
Fail fast to surface problems early.

---

## 🌟 Why this topic matters

* Prevents **silent production bugs**
* Improves **API clarity**
* Reduces **support & on-call pain**
* Interviewers look for **engineering maturity**

---

## ✅ You’ve completed the FULL roadmap 🎉

You now have:

* 📚 **15 topic-wise `.md` files**
* 🧠 **Senior-level Java mastery**
* 🧪 **Live interview screen-share questions**
* 📤 **Ready GitHub / LinkedIn content**

---

### 🔜 What would you like next?

Reply with one option:

* **`REVISION`** → Rapid revision mode (mixed questions)
* **`MOCK`** → Mock senior backend interview (Java-only)
* **`SYSTEM`** → Java + Backend system-design traps
* **`EXPORT`** → Combine all into one clean GitHub repo structure

Your call.
