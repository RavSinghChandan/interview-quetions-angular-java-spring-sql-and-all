Locked.
Proceeding with **Topic 2** exactly in the same strict format.

---

## 📄 **File name:** `java-overloading-dsa.md`

# **DSA Questions – Method Resolution & Overloading (Java)**

---

### **DSA 1**

```java
void m(long l) { System.out.println("long"); }
void m(Integer i) { System.out.println("Integer"); }

m(5);
```

**Answer:**

```
long
```

**Explanation:**
Primitive widening (`int → long`) is preferred over boxing.

---

### **DSA 2**

```java
void m(int i) { System.out.println("int"); }
void m(long l) { System.out.println("long"); }

m(5);
```

**Answer:**

```
int
```

**Explanation:**
Exact primitive match has highest priority.

---

### **DSA 3**

```java
void m(Integer i) { System.out.println("Integer"); }
void m(int... i) { System.out.println("varargs"); }

m(5);
```

**Answer:**

```
Integer
```

**Explanation:**
Boxing is chosen before varargs.

---

### **DSA 4**

```java
void m(long l) { System.out.println("long"); }
void m(Integer i) { System.out.println("Integer"); }

Integer x = 10;
m(x);
```

**Answer:**

```
Integer
```

**Explanation:**
Exact reference match is chosen; no unboxing + widening.

---

### **DSA 5**

```java
void m(Integer i) { System.out.println("Integer"); }
void m(Long l) { System.out.println("Long"); }

m(null);
```

**Answer:**

```
Compile-time error
```

**Explanation:**
`null` matches both reference types → ambiguity.

---

### **DSA 6**

```java
void m(int i) { System.out.println("int"); }
void m(Integer i) { System.out.println("Integer"); }

m(null);
```

**Answer:**

```
Integer
```

**Explanation:**
`null` cannot be passed to a primitive type.

---

### **DSA 7**

```java
void m(Integer i) {
    System.out.println(i + 1);
}

m(null);
```

**Answer:**

```
NullPointerException
```

**Explanation:**
Auto-unboxing of `null` happens at runtime.

---

### **DSA 8**

```java
void m(Long l) { System.out.println("Long"); }

m(10);
```

**Answer:**

```
Compile-time error
```

**Explanation:**
Java does not allow `int → long → Long` in one call.

---

### **DSA 9**

```java
void m(char c) { System.out.println("char"); }
void m(int i) { System.out.println("int"); }

m('A');
```

**Answer:**

```
char
```

**Explanation:**
Exact match beats primitive widening.

---

### **DSA 10**

```java
void m(Object o) { System.out.println("Object"); }
void m(String s) { System.out.println("String"); }

m("Java");
```

**Answer:**

```
String
```

**Explanation:**
Most specific applicable method is chosen.

---

### **DSA 11**

```java
void m(int i) { System.out.println("int"); }
void m(int... i) { System.out.println("varargs"); }

m(5);
```

**Answer:**

```
int
```

**Explanation:**
Varargs is selected only if no other match exists.

---

### **DSA 12**

```java
void m(int i) { System.out.println("int"); }
void m(Integer i) { System.out.println("Integer"); }

m(10);
```

**Answer:**

```
int
```

**Explanation:**
Primitive exact match is preferred over boxing.

---

Send **`3`** when ready, and I’ll generate **Topic 3 – Object Creation, Memory & JVM Basics** in the same format.
