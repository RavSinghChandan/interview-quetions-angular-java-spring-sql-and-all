Perfect 👍
Starting **Topic 3**, and I’ll make it **as colorful and readable as possible in `.md`**, while keeping it **strictly interview-usable**.

No `main`, no class, pure **output-based Java DSA**, senior-backend level.

---

## 📄 **File name:** `java-memory-dsa.md`

# 🧠 **DSA Questions – Object Creation, Memory & JVM Basics (Java)**

*(Senior Backend | Barclays / Enterprise Java)*

---

## 🎯 DSA 1 — Stack vs Heap (Object Creation)

```java
String a = "java";
String b = new String("java");
System.out.println(a == b);
```

### ✅ Answer

```
false
```

### 🧠 Explanation

String literal goes to **String Pool**, `new` creates a **heap object**.
References are different.

---

## 🎯 DSA 2 — String Pool Reuse

```java
String a = "backend";
String b = "backend";
System.out.println(a == b);
```

### ✅ Answer

```
true
```

### 🧠 Explanation

String literals are **interned** and reused from the String Pool.

---

## 🎯 DSA 3 — Explicit Heap Allocation

```java
String a = new String("api");
String b = new String("api");
System.out.println(a == b);
```

### ✅ Answer

```
false
```

### 🧠 Explanation

Each `new` creates a **separate heap object**.

---

## 🎯 DSA 4 — `intern()` Behavior

```java
String a = new String("jvm");
String b = a.intern();
System.out.println(a == b);
```

### ✅ Answer

```
false
```

### 🧠 Explanation

`intern()` returns the pooled reference, not the heap object.

---

## 🎯 DSA 5 — Stack Variable vs Heap Object

```java
int x = 10;
Integer y = 10;
System.out.println(x == y);
```

### ✅ Answer

```
true
```

### 🧠 Explanation

`y` is **unboxed** to `int` before comparison.

---

## 🎯 DSA 6 — Wrapper Cache (Memory Optimization)

```java
Integer a = 100;
Integer b = 100;
System.out.println(a == b);
```

### ✅ Answer

```
true
```

### 🧠 Explanation

Wrapper cache range `-128 to 127` reuses objects.

---

## 🎯 DSA 7 — Wrapper Cache Boundary

```java
Integer a = 200;
Integer b = 200;
System.out.println(a == b);
```

### ✅ Answer

```
false
```

### 🧠 Explanation

Values outside cache create **new heap objects**.

---

## 🎯 DSA 8 — Object Mutation via Reference

```java
StringBuilder sb = new StringBuilder("java");
StringBuilder ref = sb;
ref.append(" backend");
System.out.println(sb.toString());
```

### ✅ Answer

```
java backend
```

### 🧠 Explanation

Both references point to the **same heap object**.

---

## 🎯 DSA 9 — Immutability vs Mutability

```java
String s = "java";
s.concat(" backend");
System.out.println(s);
```

### ✅ Answer

```
java
```

### 🧠 Explanation

`String` is immutable; result is discarded.

---

## 🎯 DSA 10 — Escape Analysis Effect (Conceptual)

```java
Object o = new Object();
System.out.println(o.hashCode());
```

### ✅ Answer

```
(Some integer value)
```

### 🧠 Explanation

Object may be **stack-allocated or optimized away** by JVM
(based on escape analysis).

---

## 🎯 DSA 11 — Garbage Collection Eligibility

```java
Object o = new Object();
o = null;
```

### ✅ Answer

```
Object becomes eligible for GC
```

### 🧠 Explanation

No active references → eligible (not immediately collected).

---

## 🎯 DSA 12 — Final Reference Trap

```java
final StringBuilder sb = new StringBuilder("api");
sb.append(" service");
System.out.println(sb);
```

### ✅ Answer

```
api service
```

### 🧠 Explanation

`final` locks the **reference**, not the object.

---

## 🎯 DSA 13 — Heap Pressure via Autoboxing

```java
Integer x = 0;
for(int i = 0; i < 3; i++) {
    x++;
}
System.out.println(x);
```

### ✅ Answer

```
3
```

### 🧠 Explanation

Each `x++` creates a **new Integer object**.

---

## 🎯 DSA 14 — String Concatenation Trap

```java
String s = "java";
s = s + " backend";
System.out.println(s);
```

### ✅ Answer

```
java backend
```

### 🧠 Explanation

Creates **multiple intermediate String objects**.

---

## 🎯 DSA 15 — StringBuilder Optimization

```java
StringBuilder sb = new StringBuilder("java");
sb.append(" backend");
System.out.println(sb.toString());
```

### ✅ Answer

```
java backend
```

### 🧠 Explanation

Uses **single mutable object**, no extra allocations.

---

## 🌟 Why this topic is critical for Senior Backend

* Prevents **memory leaks**
* Helps with **GC pressure**
* Improves **high-throughput system performance**
* Interviewers check this to see if you **understand the JVM**

---

### ▶️ Next step

Reply with **`4`** to continue with:

## 🔥 `equals()` & `hashCode()` — **Production Bug Factory**

Same colorful `.md`, same strict DSA format.
