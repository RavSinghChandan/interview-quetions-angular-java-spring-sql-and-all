Locked ✅
Proceeding with **Topic 5** in the same **colorful `.md`**, **strict DSA**, **interview-ready** format.

---

## 📄 **File name:** `java-strings-dsa.md`

# 🧠 **DSA Questions – Strings & String Pool (Java)**

*(Senior Backend | Uber / Enterprise Java – Rule-Breaking Cases)*

---

## 🎯 DSA 1 — Rule break: Compile-time vs runtime concatenation—reference equality

```java
String a = "java" + "backend";
String b = "java" + (new String("backend"));
System.out.println(a == "javabackend");
System.out.println(b == "javabackend");
```

**Answer:**

```
true
false
```

**Explanation:**
Compile-time constants are folded and interned. Runtime concatenation creates new heap object. Rule "same string = same reference" breaks when one is computed.

---

## 🎯 DSA 2 — Rule break: `intern()` returns pooled ref—heap object unchanged

```java
String a = new String("jvm");
String b = a.intern();
System.out.println(a == b);
System.out.println(a.equals(b));
```

**Answer:**

```
false
true
```

**Explanation:**
`intern()` returns pooled reference; `a` still refers to heap object. Rule "intern modifies caller" breaks—it returns a different reference.

---

## 🎯 DSA 3 — Rule break: Switch on String—hashCode collision (rare)

```java
// Two different Strings can have same hashCode
// switch uses hashCode + equals; collision handled
String s1 = "Aa";
String s2 = "BB";
System.out.println(s1.hashCode() == s2.hashCode());
```

**Answer:**

```
true
```

**Explanation:**
Hash collision: "Aa" and "BB" have same hashCode (2112). Rule "hashCode uniquely identifies" breaks.

---

## 🎯 DSA 4 — Rule break: `replace` returns new String—immutability

```java
String s = "java";
s.replace('a', 'x');
System.out.println(s);
```

**Answer:**

```
java
```

**Explanation:**
replace() returns new String; original unchanged. Rule "replace modifies in place" breaks for String.

---

## 🎯 DSA 5 — Rule break: `substring` shares array (pre-Java 7)

```java
// Pre-Java 7: substring shared backing char[]
// Keeping "ja" kept entire "javabackend" in memory
```

**Answer:**

```
Memory leak risk
```

**Explanation:**
Pre-Java 7 substring shared char array. Rule "substring is independent copy" broke—fixed in Java 7+.

---

## 🎯 DSA 6 — Rule break: String concatenation in loop—O(n²)

```java
String s = "";
for(int i = 0; i < 100; i++) {
    s += i;
}
```

**Answer:**

```
~100 intermediate Strings created
```

**Explanation:**
Each `+=` creates new String. Rule "concatenation is O(1)" breaks in loops.

---

## 🎯 DSA 7 — Rule break: `trim()` doesn't remove all whitespace

```java
String s = "\u00A0";  // Non-breaking space
System.out.println(s.trim().equals(s));
System.out.println(s.strip().equals(s));  // Java 11
```

**Answer:**

```
true (trim doesn't remove)
false (strip removes)
```

**Explanation:**
trim() removes only chars <= 0x20. Unicode spaces like \u00A0 remain. Rule "trim removes whitespace" breaks for Unicode.

---

## 🎯 DSA 8 — Rule break: `StringBuilder` not thread-safe

```java
StringBuilder sb = new StringBuilder();
// Multiple threads appending → corrupted state
```

**Answer:**

```
Race condition, data corruption
```

**Explanation:**
StringBuilder has no synchronization. Rule "mutable = use carefully" breaks in concurrent context.

---

## 🎯 DSA 9 — Rule break: `format` vs `valueOf` for null

```java
System.out.println(String.valueOf(null));
System.out.println(String.format("%s", (Object)null));
```

**Answer:**

```
Compile error (ambiguous)
"null" (String)
```

**Explanation:**
valueOf(null) is ambiguous (char[] vs Object). format prints "null". Rule "null converts to 'null'" has edge cases.

---

## 🎯 DSA 10 — Rule break: `split` trailing empty strings dropped

```java
String s = "a,,b,";
System.out.println(Arrays.toString(s.split(",")));
```

**Answer:**

```
[a, , b]
```

**Explanation:**
Trailing empty strings are dropped by default. Rule "split preserves all parts" breaks.

---

## 🎯 DSA 11 — Rule break: `split` with limit

```java
String s = "a,b,c";
System.out.println(Arrays.toString(s.split(",", 2)));
```

**Answer:**

```
[a, b,c]
```

**Explanation:**
Limit 2 returns max 2 parts; rest stays together. Rule "split on every delimiter" breaks with limit.

---

## 🎯 DSA 12 — Rule break: `compareTo` for equals—wrong for sorting nulls

```java
String a = "a";
String b = null;
a.compareTo(b);  // NPE
```

**Answer:**

```
NullPointerException
```

**Explanation:**
compareTo doesn't handle null. Rule "compareTo for ordering" breaks when null is involved—use Comparator.nullsFirst.

---

## 🌟 Why this topic matters

* Prevents **memory bloat**
* Improves **throughput**
* Avoids **hidden performance regressions**
* Interviewers expect mastery here

---

### ▶️ Next step

Reply with **`6`** to continue:

## 🔥 **Collections Framework – Fundamentals**

Same colorful `.md`, same strict DSA interview format.
