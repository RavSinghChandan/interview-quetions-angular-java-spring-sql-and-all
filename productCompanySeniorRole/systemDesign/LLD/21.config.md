Excellent. Now we move into a topic that **strongly signals real production experience** when mentioned in interviews:

> **Configuration-Driven Design**

Most candidates hardcode values.
Senior engineers design systems that can be **controlled at runtime** without redeploying.

This is a major indicator of engineering maturity.

---


# STEP 21 — CONFIGURATION DESIGN (RUNTIME FLEXIBILITY)

Production systems must change behavior without code changes.

Configuration allows that.

Senior engineers always design config-driven systems.

---

# 1️⃣ WHAT IS CONFIGURATION?

Configuration = externalized system behavior settings.

Instead of hardcoding values in code,
store them outside.

Example:

Bad:
timeout = 5000

Good:
timeout = config.get("timeout")

---

# 2️⃣ WHY CONFIG MATTERS

Hardcoded systems:

- require redeploy to change
- risky updates
- slow iteration
- operational pain

Config-driven systems:

- flexible
- safe
- fast to modify
- production friendly

---

# 3️⃣ TYPES OF CONFIGURATION

Must know categories:

Runtime config  
Environment config  
Feature flags  
Secrets  
System tuning  

---

---

# 4️⃣ RUNTIME CONFIG

Values change while system running.

Examples:

- timeout limits
- retry count
- rate limits
- cache TTL

Used to tune system live.

---

---

# 5️⃣ ENVIRONMENT CONFIG

Different environments require different values.

Examples:

DEV → debug logs  
PROD → minimal logs  

Never hardcode environment values.

---

---

# 6️⃣ FEATURE FLAGS (VERY IMPORTANT)

Feature flag = enable/disable feature without deploy.

Used for:

- gradual rollout
- A/B testing
- instant rollback
- testing in production

Senior candidates mention feature flags.

---

---

# 7️⃣ SECRETS CONFIG

Sensitive values must never be hardcoded.

Examples:

- API keys
- DB passwords
- tokens

Store securely:

vault
secret manager
env variables

Mentioning this shows security awareness.

---

---

# 8️⃣ CONFIG SOURCES

Common config sources:

- environment variables
- config files
- centralized config service
- database
- remote config server

---

---

# 9️⃣ DYNAMIC CONFIG UPDATE

Advanced systems support:

Live config reload
without restart.

Example:

Change rate limit from 100 → 200 live.

Mentioning dynamic config = strong signal.

---

---

# 🔟 CONFIG VALIDATION

Invalid config can crash system.

Always validate config on load.

Example:

Timeout cannot be negative.

---

---

# 1️⃣1️⃣ DEFAULT VALUES

Config should always have safe defaults.

System must work even if config missing.

---

---

# 1️⃣2️⃣ CONFIG VERSIONING

Configs evolve.

Store versions so changes can be rolled back.

---

---

# 1️⃣3️⃣ CONFIG SCOPE

Configs can apply to:

- system wide
- service level
- user level
- request level

Senior engineers define scope carefully.

---

---

# 1️⃣4️⃣ CONFIG VS CODE RULE

Put in config if:

- value may change
- environment dependent
- tunable parameter

Keep in code if:

- logic
- algorithm
- rules

---

---

# 1️⃣5️⃣ CONFIG ANTI-PATTERNS

Avoid:

- hardcoding values
- storing secrets in code
- no defaults
- no validation
- config scattered everywhere

---

---

# 1️⃣6️⃣ REAL INTERVIEW EXAMPLE

Design Rate Limiter

Must mention:

rate limit value should be configurable.

Why?

Different customers need different limits.

---

---

# 1️⃣7️⃣ CONFIG CHECKLIST

Before finishing design verify:

✔ no hardcoded values  
✔ env-specific config supported  
✔ runtime config possible  
✔ secrets protected  
✔ defaults defined  

---

---

# 1️⃣8️⃣ SENIOR INSIGHT

Bad systems require redeploy for changes.

Good systems require config update.

---

---

# 🏆 INTERVIEW POWER SENTENCE

Say:

"I’ll make this configurable so it can be tuned without redeploying."

Interviewers instantly recognize production experience.

---

---

# 🎯 PRACTICE DRILL

Pick system daily:

Make these configurable:

- timeout
- retry count
- limits
- logging level

Practice until automatic.

---

---

# FINAL PRINCIPLE

Configuration separates:

behavior
from
deployment.

That is production-grade engineering.
```

---

✅ **Next file:** `STEP 22 — versioning.md`
This is a very strong senior-level topic: how systems evolve safely without breaking users.

Reply **22** when ready.
